/**
 * Fonts
 */
const fonts = [];

/**
 * Images
 */
const images = [];

/**
 * WebGL
 */
const webgl = [
    {
        type: 'glb',
        name: 'archiveBlock',
        path: '/models/scene_test.glb',
    },
];

const resources = [...fonts, ...images, ...webgl];

export default resources;
