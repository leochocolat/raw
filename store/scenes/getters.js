const getters = {
    active(state) {
        return state.active;
    },

    menu(state) {
        return state.menu;
    },

    blurValue(state) {
        return state.blurValue;
    },
};

export default getters;
